# ข้อเสนอ: ระบบติดตามค่าไฟอัตโนมัติผ่านเว็บ AMR (PEA AMR Web Scraper)

**เป้าหมายใหม่:** สร้างระบบดึงข้อมูลอัตโนมัติจากเว็บไซต์ **PEA AMR** เพื่อให้ได้ข้อมูลที่แม่นยำตรงกับบิลค่าไฟที่สุด โดยไม่ต้องติดตั้งอุปกรณ์เพิ่มเติม (Hardware-less) และแสดงผลในรูปแบบ Dashboard ที่ดูง่ายกว่าหน้าเว็บเดิม

## วิเคราะห์ข้อจำกัดและโอกาส (Constraint Analysis)

- **ข้อจำกัด:** ไม่สามารถติดตั้งตู้ Control หรือมิเตอร์แยก (No IoT/Modbus)
- **โอกาส:** มีรหัสเข้าใช้งานเว็บ AMR (Metelink/AMR Web) ซึ่งมีข้อมูลความละเอียด 15 นาทีอยู่แล้ว
- **เป้าหมาย:** "ข้อมูลต้องเท่ากับ Web AMR มากที่สุด" -> วิธีที่ดีที่สุดคือการดึงจาก Source โดยตรง

## แนวทางแก้ไขใหม่ (Revised Solution: The "Crawler" Approach)

เราจะเปลี่ยนจากการติดเซนเซอร์ เป็นการใช้ **"หุ่นยนต์ซอฟต์แวร์ (RPA/Web Crawler)"** ทำหน้าที่ล็อกอินเข้าไปอ่านค่าแทนคน ตลอด 24 ชั่วโมง

### สถาปัตยกรรมระบบใหม่ (New Architecture)

```mermaid
graph TD
    User((User)) --x Web[เว็บ PEA AMR] : ไม่ต้องกดดูเองแล้ว

    subgraph "Your Server / PC (On-Premise)"
        Bot[Python RPA Bot] -->|1. Login & Scrape ทุก 15 นาที| Web
        Bot -->|2. Extract Data| RawData[ข้อมูลดิบ JSON]
        RawData -->|3. Save| DB[(Database)]
    end

    DB -->|4. Visualize| Dash[Custom Dashboard]
    DB -->|5. Alert| Line[Line Notify]
```

### ขั้นตอนการทำงานของบอท (Automation Workflow)

1.  **Login:** บอทใช้ User/Pass ที่ให้มา ล็อกอินเข้า `amr.pea.co.th` โดยจำลองตัวเป็น navegador (Chrome/Edge) เพื่อความแนบเนียน
2.  **Navigate:** กดเข้าไปที่เมนู "Load Profile" หรือ "Meter Reading" ตาม URL ที่ระบุ
3.  **Extraction:** ดึงตารางข้อมูลช่วงเวลาล่าสุด (Last 15-min interval) และค่า Demand ปัจจุบัน
4.  **Action:**
    - ถ้าค่า Demand ใกล้แตะ Control Line -> **ส่ง Line แจ้งเตือนทันที**
    - บันทึกข้อมูลลง Database เพื่อทำกราฟย้อนหลัง

## มาตรการความปลอดภัย (Security Measures) **สำคัญมาก**

เนื่องจากต้องใช้รหัสผ่านจริงในการเข้าถึงข้อมูล ความปลอดภัยจึงเป็นสิ่งสำคัญที่สุด:

1.  **การจัดการรหัสผ่าน (Zero-Trust Credential Storage):**

    - **ห้าม** ฝังรหัสผ่านไว้ในโค้ด (Source Code) เด็ดขาด
    - รหัสผ่านจะถูกเก็บในไฟล์ **`.env`** (Environment Variable) ซึ่งจะอยู่แค่ในเครื่องคอมพิวเตอร์ของคุณเท่านั้น
    - ไฟล์นี้จะถูกตั้งค่า `Git Ignore` ไม่ให้หลุดออกไปภายนอกหรือขึ้น Cloud

2.  **ป้องกันการโดนบล็อก (Anti-Blocking Strategy):**

    - ระบบจะตั้งเวลาดึงข้อมูลทุก **15-20 นาที** เท่านั้น (สอดคล้องกับรอบข้อมูลของ AMR) เพื่อไม่ให้ Server สงสัยว่าเป็นการโจมตี (Spamming)
    - ใส่ระบบ **Random Delay** (เช่น รอ 5-15 วินาทีก่อนกดปุ่ม) ให้ดูเหมือนมนุษย์ใช้งานจริง
    - หาก Login ผิดพลาดเกิน 3 ครั้ง ให้ระบบหยุดทำงานทันที (Circuit Breaker) เพื่อป้องกัน Account Locked

3.  **Data Privacy:**
    - ข้อมูลค่าไฟทั้งหมดจะถูกเก็บใน Database ภายในเครื่อง (Localhost) เท่านั้น ไม่มีการส่งข้อมูลออกไปภายนอก

## แผนการดำเนินงาน (Implementation Plan)

### Phase 1: สร้างบอททดสอบ (Proof of Concept Crawler) - **ทำทันที**

- [ ] เขียน Script Python (ใช้ Library `Playwright` หรือ `Selenium`)
- [ ] ทดสอบ Login ด้วย User: `0200xxxxxxxxxx`
- [ ] ทดสอบดึงค่าล่าสุดมาแสดงบนหน้าจอ Console
- [ ] **Challenge:** ต้องจัดการเรื่อง Session Timeout และ CAPTCHA (ถ้ามี)

### Phase 2: เชื่อมต่อ Dashboard (Integration)

- [ ] นำข้อมูลจากบอท ส่งเข้า API ของ Dashboard เดิมที่เราทำเสร็จแล้ว
- [ ] ปรับหน้าจอ Dashboard ให้แสดง "เวลาอัปเดตล่าสุดจาก PEA" เพื่อความมั่นใจ

## ข้อดีของการใช้วิธีนี้

1.  **ต้นทุนต่ำมาก:** ไม่ต้องซื้อตู้หรือเดินสายไฟ ใช้แค่คอมพิวเตอร์ 1 เครื่องเน็ตเสถียรๆ
2.  **ข้อมูลแม่นยำ 100%:** เพราะดึงมาจากแหล่งเดียวกับที่ PEA ใช้คิดเงิน
3.  **อัตโนมัติ:** ไม่ต้องจ้างคนมานั่งเฝ้าจอ Log in ทุกชั่วโมง

## สิ่งที่ต้องตระหนัก (Trade-offs)

- **ความล่าช้า (Latency):** ข้อมูลบนเว็บ AMR มักจะไม่ใช่ Real-time วินาทีต่อวินาที (อาจดีเลย์ 15-30 นาที ขึ้นอยู่กับโมเด็มมิเตอร์) ซึ่งเราแก้ไขไม่ได้ แต่ระบบจะช่วย "เฝ้าระวังแนวโน้ม" ได้ดีที่สุดเท่าที่เป็นไปได้ในเงื่อนไขนี้

---

**Next Step:** ให้ผมเริ่มเขียน **Script ทดสอบการ Login และดึงข้อมูล (Crawler)** เลยไหมครับ?

## ขั้นตอนต่อไป (Next Steps)

- **ตรวจสอบ:** แนวคิดนี้ตรงกับสิ่งที่คุณมองหาหรือไม่ครับ?
- **การตัดสินใจ:** ให้ผมเริ่มทำ **"ระบบจำลอง Dashboard" (Simulation)** ให้ดูก่อนไหมครับ? เพื่อใช้เป็นต้นแบบ (Prototype) ไปเสนอผู้บริหารว่าระบบของจริงหน้าตาจะเป็นอย่างไร และช่วยลดค่าไฟได้อย่างไร
